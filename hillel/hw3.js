const fstVal = +prompt('Pls type fst. number',0);
const secVal = +prompt('Pls type sec. number',0);

//Користувач ввів 2 і 2:
alert(`Користувач ввів ${fstVal} і ${secVal}:
    sum=${fstVal+secVal}
    diff=${fstVal-secVal}
    mult=${fstVal*secVal}
    div=${(fstVal/secVal)} `); 
